/**
 * this module is placeholder objects to keep the auto regex split intacted. 
 * so that indivisual styles can be applied and remain as in configuration. 
 * 
 * old methods were using symbols to find exact position in array of renderOptions 
 * and set the contentText, which has been refactored into using references 
 * of the object to assign the contentText in renderOption.after object.
 * 
 */

export const columnsOfCol = 0;
export const columnsOfZCol = 0;
export const lineNumberOfLn = 0;
export const characterCountOfChar = 0;

export const multiCursorLineCol = 0;
export const multiCursorLineZCol = 0;
export const multiCursorLineNth = 0;
export const multiCursorLineNumber = 0;
export const multiCursorLineCount = 0;
export const multiCursorLineCountFull = 0;
export const multiCursorLineCharacter = 0;
